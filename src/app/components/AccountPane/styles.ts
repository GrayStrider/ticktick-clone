import styled from 'styled-components';

export const Wrapper = styled.div`
    .row {
    display: flex;
    align-items: center;
    
    padding: 0.5em;
    
    .buttons {
      margin-left: auto;
    }
    
    .icon {
      margin-left: 0.5em;
    }
    
  }
`;
