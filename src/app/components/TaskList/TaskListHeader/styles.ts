import styled from 'styled-components';

export const Wrapper = styled.span`
  padding: 0.5em 1em 0 1em;
  display: flex;
  flex-direction: row;
  
  .header {
    display: inline;
    margin: 0;
    flex: auto;
  }
  ;
`;
